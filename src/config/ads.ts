/**
 * AdMob ad-unit IDs.
 *
 * Leave these EMPTY during development — the app then shows Google's official
 * TEST ads (safe to view/click). Before publishing, paste your real AdMob
 * ad-unit IDs here (from AdMob → your app → Ad units), e.g.
 *   banner: 'ca-app-pub-1234567890123456/1234567890'
 *
 * Also set your real App ID in `app.json` under `react-native-google-mobile-ads`.
 */
export const AD_UNITS = {
  /** Anchored adaptive banner (above the bottom tabs, etc.). */
  banner: '',
  /** Full-screen video interstitial (shown once after the splash). */
  interstitial: '',
};
