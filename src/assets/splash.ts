/**
 * Splash image source. Portrait / mobile-ratio image; the SplashScreen adapts
 * it to both phones and tablets. Set to `null` to fall back to the branded
 * gradient placeholder.
 */
export const splashImage: number | null = require('./splash.png');
